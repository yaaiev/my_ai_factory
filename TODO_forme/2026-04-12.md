# 2026-04-12 TODO

## 1. 删除旧 seed 文件

- 状态：待执行
- 原因：项目运行代码已经切换为读取 `projects/ai-intel-terminal/data/seed_persons.json`，旧的 YAML 文件不再被代码消费，继续保留会造成双份 seed 来源。
- 命令：

```bash
rm /Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/data/seed_persons.yaml
```

- 完成标记：路径 `/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/data/seed_persons.yaml` 不存在

## 2. 运行真实 RSS 正式日报生成

- 状态：待执行
- 原因：当前沙箱网络受限，我无法直接访问外网 feed；需要你本地运行真实 RSS pipeline，生成只含真实链接的正式中文日报。
- 命令：

```bash
python3 /Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/pipelines/rss_news_demo.py
```

- 完成标记：文件 `/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/evidence/2026-04-12-rss-daily-brief-zh.md` 存在，且其中链接为真实来源链接

## 3. 运行真实 GitHub 正式日报生成

- 状态：待执行
- 原因：当前沙箱网络受限，我无法直接访问 GitHub Atom feed；需要你本地运行真实 GitHub pipeline，生成只含真实链接的正式中文日报。
- 命令：

```bash
python3 /Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/pipelines/github_activity_demo.py
```

- 完成标记：文件 `/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/evidence/2026-04-12-github-daily-brief-zh.md` 存在，且其中链接为真实来源链接

## 4. 删除旧 source catalog YAML

- 状态：待执行
- 原因：GitHub demo 已改为读取 `projects/ai-intel-terminal/data/source_catalog.json`，旧的 YAML catalog 不再作为运行时配置源，继续保留会造成双份 source 配置。
- 命令：

```bash
rm /Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/data/source_catalog.yaml
```

- 完成标记：路径 `/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/data/source_catalog.yaml` 不存在

## 5. 安装 Twitter/X 浏览器观察层依赖

- 状态：待执行
- 原因：真实 `Twitter/X` 观察层已实现，但当前共享项目环境里还没有 `playwright`，无法复用已登录浏览器态做真实抓取。
- 命令：

```bash
cd /Users/dev/yana_prj/my_ai_factory/projects
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install playwright
python -m playwright install chromium
```

- 完成标记：命令 `cd /Users/dev/yana_prj/my_ai_factory/projects && source .venv/bin/activate && python -c "import playwright"` 能正常执行且不报错

## 6. 设置已登录浏览器 profile 并运行真实 Twitter/X 正式 pipeline

- 状态：待执行
- 原因：系统现在默认优先使用 `CDP`，只有探测不到 `9222` 调试端口时才退回 `X_BROWSER_USER_DATA_DIR`。
- 命令：

```bash
cd /Users/dev/yana_prj/my_ai_factory/projects
source .venv/bin/activate
export X_BROWSER_USER_DATA_DIR="/替换成你的 Chromium 或 Chrome 用户数据目录"
python /Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/pipelines/twitter_observer_demo.py
```

- 完成标记：文件 `/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/evidence/2026-04-12-twitter-investment-signals-formal-zh.md` 存在，且 `/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/evidence/twitter_observer_pipeline_summary.json` 中 `observed_signal_count > 0`

## 7. 用 CDP 连接已打开的 Chrome 运行真实 Twitter/X pipeline

- 状态：待执行
- 原因：`CDP` 已成为默认优先路径，可绕开真实 Chrome profile 的锁冲突与 Crashpad 权限问题。
- 命令：

```bash
open -na "Google Chrome" --args --remote-debugging-port=9222
cd /Users/dev/yana_prj/my_ai_factory/projects
source .venv/bin/activate
export X_BROWSER_CDP_URL="http://127.0.0.1:9222"
python /Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/pipelines/twitter_observer_demo.py
```

- 完成标记：`/Users/dev/yana_prj/my_ai_factory/projects/ai-intel-terminal/evidence/twitter_observer_pipeline_summary.json` 中 `browser_mode = "cdp"` 且 `observed_signal_count > 0`
